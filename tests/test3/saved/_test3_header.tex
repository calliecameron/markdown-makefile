\usepackage{bold-extra}
\usepackage[perpage,symbol*]{footmisc}

% This makes horizontal rules for section breaks appear as three stars, rather
% than a solid line. Unfortunately 'rule' is used in other places, such as
% footnotes - hence the conditional gymnastics. Hopefully it doesn't break
% anything else.
\newif\iffootnote
\let\Footnote\footnote
\renewcommand\footnote[1]{\begingroup\footnotetrue\Footnote{#1}\endgroup}

\let\OldRule\rule
\renewcommand{\rule}[2]{\iffootnote\OldRule{#1}{#2}\else* * *\fi}

\special{pdf:trailerid [
    <be42db5898a8e2eb0bd3f4e44dcab8db>
    <be42db5898a8e2eb0bd3f4e44dcab8db>
]}
