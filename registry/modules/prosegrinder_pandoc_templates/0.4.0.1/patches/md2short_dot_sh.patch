--- bin/md2short.sh
+++ bin/md2short.sh
@@ -81,6 +81,10 @@ else
     TEMPLATE='template.docx'
 fi

+if [[ -z "${PANDOC}" ]]; then
+    PANDOC=pandoc
+fi
+
 # Create a temporary data directory
 echo "Creating temporary directory."
 export PANDOC_DATA_DIR
@@ -95,7 +99,7 @@ echo "Files extracted."

 # Run pandoc
 echo "Running Pandoc."
-pandoc \
+"${PANDOC}" \
   --from=markdown \
   --to=docx \
   --lua-filter="$SHUNN_SHORT_STORY_DIR/shunnshort.lua" \
