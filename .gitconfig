[diff "catdoc"]
textconv = catdoc
binary = true

[diff "hexdump"]
textconv = hexdump -v -C
binary = true

[diff "pdfsummary"]
textconv = bin/pdfsummary
binary = true

[diff "zip"]
textconv = unzip -q -c -a
binary = true
