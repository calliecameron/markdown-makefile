[diff "binary"]
textconv = bash -c 'bazel run --ui_event_filters=-info,-stdout,-stderr --noshow_progress @markdown_makefile//markdown_makefile/utils:bindump -- "${0}"'
cachetextconv = true
binary = true

[diff "doc"]
textconv = bash -c 'bazel run --ui_event_filters=-info,-stdout,-stderr --noshow_progress @markdown_makefile//markdown_makefile/utils:docdump -- "${0}"'
cachetextconv = true
binary = true

[diff "pdf"]
textconv = bash -c 'bazel run --ui_event_filters=-info,-stdout,-stderr --noshow_progress @markdown_makefile//markdown_makefile/utils:pdfdump -- "${0}"'
cachetextconv = true
binary = true

[diff "zip"]
textconv = bash -c 'bazel run --ui_event_filters=-info,-stdout,-stderr --noshow_progress @markdown_makefile//markdown_makefile/utils:zipdump -- "${0}"'
cachetextconv = true
binary = true
